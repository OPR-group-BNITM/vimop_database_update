params {
    taxa_config = "REPLACE_THIS"
    fasta_sequences = "REPLACE_THIS"
    out_dir = 'REPLACE_THIS'
    output_version = 'REPLACE_THIS'

    //conda_env = "${baseDir}/envs/alignorient.yml"

    filter_max_n_share = 0.01
    filter_min_relative_length = 0.8
    cdhit_threshold = 0.98

    minimap {
        kmer_length=11
        window_size=5
        p=0.6
    }
}

process {
    //conda = params.conda_env
    // You can also specify other global settings here
    executor = 'local'
    cpus = 2
    memory = '4 GB'
}

profiles {
    standard {
        // 
    }
}
